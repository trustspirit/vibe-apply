# Docker ignore file - minimal exclusions
# Only exclude what we don't need, keep everything else including pnpm-workspace.yaml

node_modules
**/node_modules
dist
**/dist
build
**/build
coverage
**/coverage
.git
.env
.env.*
!.env.example
.DS_Store
*.log
