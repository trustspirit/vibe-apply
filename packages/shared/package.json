{
  "name": "@vibe-apply/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Vibe Apply",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "echo 'No linting configured for shared package'",
    "lint:fix": "echo 'No linting configured for shared package'",
    "format": "echo 'No formatting configured for shared package'",
    "format:check": "echo 'No formatting configured for shared package'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "class-validator": "^0.14.1",
    "class-transformer": "^0.5.1"
  }
}