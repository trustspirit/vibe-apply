/**
 * Normalize a string by trimming whitespace and converting to lowercase
 * @param value - The string to normalize
 * @returns The normalized string
 */
export declare function normalizeString(value: string | null | undefined): string;
/**
 * Normalize an email address
 * @param email - The email to normalize
 * @returns The normalized email
 */
export declare function normalizeEmail(email: string | null | undefined): string;
/**
 * Normalize a ward or stake name
 * @param name - The ward/stake name to normalize
 * @returns The normalized name
 */
export declare function normalizeWardStakeName(name: string | null | undefined): string;
